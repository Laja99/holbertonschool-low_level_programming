#include "main.h"
/**
 * _isdigit - if parameter c is digit
 * @c: the parameter
 *
 * Return: 1 if c is digit, 0 otherwise
*/
int _isdigit(int c)
{

	if (c >= 0 && c <= 9)
	{
		_putchar('1');
	}
	else
	{
		_putchar('0');
	}
	return (0);
}
