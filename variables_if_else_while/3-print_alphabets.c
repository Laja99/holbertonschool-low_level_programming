#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: if else
 *
 * Return: Always 0 (success)
*/
int main(void)
{
	char ch;

	for (ch = 'A'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
